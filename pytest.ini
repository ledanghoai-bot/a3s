[pytest]
# Chỉ collect unit test ở tests/ (CI chạy `pytest -v` không path). Các file scripts/*_test.py là
# EVIDENCE SCRIPT thực thi (chạy với PYTHONPATH=/srv + DB, vd audit_rollback_endpoint_test.py,
# rbac_strict_test.py) — KHÔNG phải unit test; nếu để pytest tự collect sẽ ImportError 'app' / side-effect.
testpaths = tests
